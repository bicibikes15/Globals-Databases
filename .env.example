# ##################################################
# #     VARIABLES DE EJEMPLO PARA BASES DE DATOS   #
# ##################################################
#
# Copia este archivo a .env (cp .env.example .env) y reemplaza los placeholders
# con tus propias contraseñas seguras.

# === Credenciales para PostgreSQL ===
# Base de datos relacional para uso general.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CAMBIAME_CON_UNA_CLAVE_SEGURA_PARA_POSTGRES
POSTGRES_DB=global_default_db

# === Credenciales para PGVector (PostgreSQL con extensión de vectores) ===
# Base de datos para IA y búsqueda semántica.
PGVECTOR_USER=postgres
PGVECTOR_PASSWORD=CAMBIAME_CON_UNA_CLAVE_SEGURA_PARA_PGVECTOR
PGVECTOR_DB=vector_default_db

# === Credenciales para MongoDB ===
# Base de datos NoSQL para datos flexibles.
MONGO_USER=mongoadmin
MONGO_PASSWORD=CAMBIAME_CON_UNA_CLAVE_SEGURA_PARA_MONGO